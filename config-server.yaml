
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/uzayilica/besiktas.git
          username: uzayilica
          password: ${GIT_KEY}
          default-label: main
server:
  port: 8888
